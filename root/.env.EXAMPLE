# Database configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=Challenge-13

# JWT secret key (for authentication)
JWT_SECRET=your_jwt_secret_key

# Any other application-specific configuration variables
